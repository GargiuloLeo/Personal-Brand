/* Estilos generales */
body {
    font-family: 'Arial', sans-serif;
    margin: 0;
    padding: 0;
    background-color: #f5f5f5;
    color: #333;
}

/* Header */
header {
    background-color: #1f1f1f;
    padding: 20px;
    display: flex;
    justify-content: space-between;
    align-items: center;
    color: white;
}

nav ul {
    list-style: none;
    margin: 0;
    padding: 0;
    display: flex;
}

nav ul li {
    margin-right: 20px;
}

nav ul li a {
    text-decoration: none;
    color: white;
}

/* Sección Hero */
.hero {
    background: url('fondo.jpg') no-repeat center center/cover;
    color: white;
    text-align: center;
    padding: 100px 20px;
    border-radius: 10px; /* Bordes redondeados */
    margin: 20px auto;
    max-width: 1200px; /* Centra el contenido */
}

.hero h1 {
    font-size: 48px;
}

.hero button {
    padding: 10px 20px;
    background-color: #ff6347; /* Color de botón */
    color: white;
    border: none;
    border-radius: 5px;
    cursor: pointer;
}

.hero button:hover {
    background-color: #e5533d; /* Hover en botón */
}

/* Sección de productos */
.productos {
    padding: 40px;
    background-color: #ffffff;
    border-radius: 10px; /* Bordes redondeados */
    margin: 20px auto;
    max-width: 1200px; /* Centra el contenido */
    border: 1px solid #ddd; /* Bordes definidos */
}

.productos h2 {
    text-align: center;
    color: #333;
    margin-bottom: 20px;
}

.grid {
    display: flex;
    justify-content: space-around;
    gap: 20px; /* Espaciado entre los elementos */
}

.producto {
    background-color: #f9f9f9; /* Color ligeramente distinto para los productos */
    padding: 20px;
    border: 1px solid #ddd; /* Borde definido para cada producto */
    border-radius: 5px;
    text-align: center;
}

.producto img {
    width: 150px;
    height: 150px;
    margin-bottom: 10px;
}

/* Sección de testimonios */
.testimonios {
    padding: 40px;
    background-color: #f0f0f0; /* Fondo gris suave */
    border-radius: 10px; /* Bordes redondeados */
    margin: 20px auto;
    max-width: 1200px; /* Centra el contenido */
    border: 1px solid #ddd; /* Bordes definidos */
    text-align: center;
}

.testimonios h2 {
    margin-bottom: 20px;
}

/* Footer */
footer {
    background-color: #1f1f1f;
    color: white;
    text-align: center;
    padding: 20px;
    margin-top: 20px;
    border-radius: 10px; /* Bordes redondeados */
}

footer .social a {
    margin: 0 10px;
    color: white;
    text-decoration: none;
}

footer .social a:hover {
    text-decoration: underline;
}

/* Estilos para móvil */
@media (max-width: 768px) {
    nav ul {
        flex-direction: column;
    }

    .grid {
        flex-direction: column;
        align-items: center;
    }
}
